# Grand Star Cup backgrounds

Add optimized base-game track images as sequential WebP files (`001.webp`,
`002.webp`, and so on). After adding them, update the GSC background count in
`frontend/src/config/leagues.ts` and run `npm run verify:media`.

